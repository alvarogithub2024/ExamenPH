import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacionfinal4.ultimo',
  appName: 'evaluacionfinal4-ultimo',
  webDir: 'www'
};

export default config;
